# 部署与服务启动

这份旧入口文档已经拆分为两份主文档：

- 本地开发与服务启动：
  - `本地开发与服务启动.md`
- 云端部署与交付：
  - `云端部署与交付.md`

阅读建议：

1. 如果目标是新人在本机起服务，先读 `本地开发与服务启动.md`
2. 如果目标是镜像构建、Docker Compose、远端一键部署或二进制交付，读 `云端部署与交付.md`

当前仓库内真实交付物位于：

- `Dockerfile`
- `web/Dockerfile`
- `deploy/docker-compose.cloud.yml`
- `deploy/athena.env.example`
- `deploy/systemd/athena.service.example`
- `scripts/build_release_bundle.sh`
- `scripts/deploy_cloud.sh`
