services:
  postgres:
    image: postgres:16
    container_name: athena-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-secure_digital}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - athena-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-secure_digital}"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7.4-alpine
    container_name: athena-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - athena-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  athena-api:
    image: ${ATHENA_IMAGE:-athena:local}
    container_name: athena-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - ./athena.env
    command: ["/bin/sh", "-lc", "/app/athena migrate && exec /app/athena api-server"]
    ports:
      - "${ATHENA_HTTP_PORT:-8090}:8080"
    volumes:
      - athena-controlplane-data:/app/config/controlplane
      - athena-shared-data:/srv/shared
    healthcheck:
      test: ["CMD", "/app/athena", "healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 10

  athena-worker:
    image: ${ATHENA_IMAGE:-athena:local}
    container_name: athena-worker
    restart: unless-stopped
    depends_on:
      athena-api:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - ./athena.env
    command: ["worker"]
    volumes:
      - athena-controlplane-data:/app/config/controlplane
      - athena-shared-data:/srv/shared

  athena-web:
    image: ${ATHENA_WEB_IMAGE:-athena-web:local}
    container_name: athena-web
    restart: unless-stopped
    profiles:
      - control-plane
    depends_on:
      athena-api:
        condition: service_healthy
    ports:
      - "${ATHENA_WEB_PORT:-5173}:80"

volumes:
  athena-postgres-data:
  athena-redis-data:
  athena-controlplane-data:
  athena-shared-data:
