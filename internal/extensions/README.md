# extensions

## 模块职责

- `internal/extensions` 负责承载可插拔的外部专项增强实现。

## 不负责什么

- 不负责 Athena 主线骨架
- 不负责主状态机
- 不直接持有外部系统主数据

## 子目录索引

- `platform/`
  - 外部平台适配器的可插拔增强实现。

## 维护提示

- 扩展层必须保持可删除、可替换。
- 删除扩展层后，Athena 主线服务仍应可运行。
